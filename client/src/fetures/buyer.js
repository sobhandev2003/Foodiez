import { createSlice } from "@reduxjs/toolkit";

const initialState={
    
}

export const buyerSlice=createSlice({
    name:"buyer",
    initialState,
    reducers:{
        
    }
}) 
// export const {setLoginBuyer}=buyerSlice.actions;
export default buyerSlice.reducer;